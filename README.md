# MediAI Healthcare Diagnosis Assistant

MediAI is a Flask-based healthcare assessment web application that uses machine learning to suggest possible health conditions from user-entered symptoms. It features a modern clinical UI, live vitals and BMI calculators, searchable symptom selection, emergency screening, offline assessment, optional Groq AI assistance, and downloadable reports.

> **Safety disclaimer:** MediAI is an educational/research project. It is not a medical diagnosis tool, emergency service, or substitute for a qualified healthcare professional.

## MediAI 2.0

The project is being upgraded from a single TF-IDF + Random Forest classifier into a more rigorous, safety-aware ML framework.

### New ML foundation

- Structured binary symptom feature matrix
- Dataset-quality audit
- Multiple model benchmarking: Logistic Regression, Random Forest, calibrated Linear SVM, and HistGradientBoosting
- Leakage-aware StratifiedGroupKFold evaluation
- Accuracy, macro precision, macro recall, macro F1, and weighted F1
- Reproducible model metadata and model card
- Backward-compatible prediction interface
- Explicit distinction between **model probability** and clinical certainty

### Safety-first assessment

- Emergency/urgent triage layer runs before routine AI guidance
- Abnormal SpO2, very high temperature, critical severity, and user-selected warning symptoms can trigger escalation
- Emergency/urgent results are not passed through as routine self-care advice
- The safety engine is a project heuristic and is not a validated clinical triage protocol

## Project structure

```text
AI-Healthcare/
├── app.py
├── predict.py
├── predict_v2.py
├── train_model.py
├── safety_engine.py
├── utils.py
├── groq_helper.py
├── report_generator.py
├── MODEL_CARD.md
├── ml/
│   ├── __init__.py
│   ├── preprocess.py
│   ├── audit_dataset.py
│   ├── train.py
│   └── evaluate.py
├── dataset/
│   └── disease_sympts_prec_full.csv
├── model/
│   ├── model.pkl
│   └── vectorizer.pkl
├── results/
│   └── # generated by the ML audit/training/evaluation scripts
├── static/
└── templates/
```

## Train MediAI 2.0

From the repository root:

```bash
python -m ml.audit_dataset
python -m ml.train
python -m ml.evaluate
```

Training creates `model/model_v2.pkl` and `model/symptom_vocabulary_v2.pkl`. Until those artifacts exist, `predict.py` automatically keeps using the original model so existing deployments remain functional.

## Technology stack

- Python / Flask
- Pandas / NumPy
- scikit-learn
- Joblib
- Groq (optional)
- ReportLab
- Matplotlib for evaluation artifacts

## Responsible use

The benchmark dataset is not a substitute for clinically collected patient data. Model performance on this dataset does not establish clinical effectiveness. Do not use a MediAI prediction as the sole basis for treatment or medication decisions. For severe, rapidly worsening, or emergency symptoms, seek professional medical care regardless of the model output.
